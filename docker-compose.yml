version: '3.8'

services:
  # Our AIS Generator Service
  ais-generator:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./output:/app/output  # Mount output directory for JSON files
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
