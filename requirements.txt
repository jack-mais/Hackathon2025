# Core dependencies
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# LLM integration for Run phase (choose one)
openai>=1.0.0              # OpenAI API (requires paid API key)
google-generativeai>=0.3.0 # Google Gemini (free tier available)
# anthropic>=0.7.0         # Claude API (alternative option)

# Data handling (lightweight)
requests>=2.31.0

# Map visualization
folium>=0.14.0

# CLI and utilities  
click>=8.0.0
rich>=13.0.0
